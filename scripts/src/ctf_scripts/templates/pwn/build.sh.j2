#!/bin/sh
set -eu

# Builds your challenge
# can be used locally for testing, but is used inside build container

cd -- "$(dirname -- "$0")"

# By default, build.Dockerfile copies `chall` out to dist
# If multiple build files are desired, copy those as well
gcc chall.c -o chall